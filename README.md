# Loader Stepper
by: Najim Riky

## Technologies
- React + Vite
- Typescript

## How to run
1. Open the terminal
2. Run `npm install`
2. Run `npm run dev`
3. Open the browser at `http://localhost:5173/`